# Server Configuration
NODE_ENV=development
PORT=3002

# Server URL (for config endpoint response)
# In production, set to your Railway URL
SERVER_URL=http://localhost:3002

# API Authentication
# These credentials are used by the main app to authenticate
MOCK_API_KEY=mock-api-dev-key
MOCK_API_SECRET=mock-api-dev-secret

# Database (Railway Postgres)
# Railway auto-injects DATABASE_URL when you add a Postgres service
DATABASE_URL="postgresql://user:password@host:5432/mock_api_db"
